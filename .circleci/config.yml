version: 2
jobs:
  test:
    docker:
      - image: circleci/golang:1.14
    steps:
      - checkout
      - run:
          command: |
            gpg --import sops_functional_tests_key.asc
            go run main.go test1
workflows:
  version: 2
  test:
    jobs:
      - test